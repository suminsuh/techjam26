# RIFT: pip install -r requirements.txt
# GPU: install the PyTorch build that matches your CUDA from https://pytorch.org

torch>=2.2
torchvision>=0.17
timm>=1.0.0
numpy>=1.26
scipy>=1.11
pillow>=10.0
pyyaml>=6.0
scikit-learn>=1.4
tqdm>=4.66
matplotlib>=3.8

# SID_Set / CIFAKE / optional holdout loaders
datasets>=2.18.0
huggingface_hub>=0.23.0
pyarrow>=15.0.0

# Demo + tests
gradio>=4.44
pytest>=8.0
