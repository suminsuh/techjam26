# Core — keep the default stack under the <2B-parameter rule.
torch>=2.2
torchvision>=0.17
timm>=1.0.0
numpy>=1.26
scipy>=1.11
pillow>=10.0
pyyaml>=6.0
scikit-learn>=1.4
tqdm>=4.66
matplotlib>=3.8

# Demo + tests (install when you need them)
gradio>=4.44
pytest>=8.0
